# movies.json 隔週更新の指示

このフォルダにある `movies.json` を、いま観られる映画の最新情報に更新してください。

## 手順

1. **今日の日付を確認し、対象期間を決める**
   - 対象は **公開中の作品が中心**。あわせて、直近1か月ほどの間に公開予定の作品も少し含める
   - `targetPeriod` は `2026年9月上旬〜10月上旬` のような表記にする
   - `nextUpdate` は **今日の14日後** を `YYYY-MM-DD` 形式で入れる

2. **作品を集める（Web検索・取得）**

   まずは公開中／近日公開のラインナップを押さえる。

   - 映画.com 公開中の映画 https://eiga.com/now/
   - 映画.com 近日公開 https://eiga.com/coming/
   - ぴあ映画 https://lp.p.pia.jp/
   - Filmarks 上映中 https://filmarks.com/list/now
   - シネマトゥデイ https://www.cinematoday.jp/

   次に、**このサイトの主役である「ムビチケ」と「入場者特典」**を必ず個別に調べる。
   ここが薄いとサイトの意味が無いので、作品ごとに公式サイトまで見に行くこと。

   - **入場特典リスト（アニメハック／映画.com）** https://anime.eiga.com/news/column/tokuten_list/
     毎月更新される特典の一覧記事。まずここを見ると効率が良い（ただしアニメ中心なので、
     実写作品の特典は別途調べる）
   - **MOVIE WALKER STORE（ムビチケ公式の販売サイト）** https://ticket.moviewalker.jp/
     作品ページは `https://ticket.moviewalker.jp/film/<番号>` の形。価格・販売状況が分かる
   - **メイジャー（映画前売券情報）** https://www.major-j.com/
     前売券の販売劇場・前売券特典がまとまっている
   - 各作品の**公式サイト**（「作品名 公式サイト」で検索）。
     公式サイトの「NEWS」「チケット」「ムビチケ」「入場者特典」「来場者特典」ページを必ず確認する
   - 「作品名 入場者特典」「作品名 ムビチケ 販売期間」で個別に検索する
   - 配給会社サイト（東宝 https://www.toho.co.jp/ 、東映 https://www.toei.co.jp/ 、
     松竹 https://www.shochiku.co.jp/ 、ワーナー、ソニー・ピクチャーズ など）

   > **注意:** `mubichike.jp` というドメインは存在しない。ムビチケの入口は `mvtk.jp` で、
   > そこから `ticket.moviewalker.jp` / `store.moviewalker.jp` に転送される。

3. **大人向け・こども向けをそれぞれ 8〜12本ずつ選ぶ**
   - `audience` は次の3種類。**親子で観られる作品は必ず `both`** にする（排他にしない）
     - `"kids"` … 小学生以下がメインで楽しめる作品（アニメ、ファミリー実写、G区分中心）
     - `"adult"` … 大人向け（小学生以下には内容的に向かない、PG12以上など）
     - `"both"` … 親子どちらでも楽しめる作品
   - 合計で **16〜24本程度**。`both` は大人・こども両方の枠に数えてよい
   - 話題作を優先しつつ、ジャンルが偏らないようにする
     （こども向け＝アニメばかりにしない。大人向け＝洋画大作ばかりにしない）
   - **単館系・ミニシアターのみの作品は原則除外**する（横浜周辺で観られないため）

4. **ポスター画像を TMDb から取得する**

   `config.local.json` の `tmdbApiKey` を読み、TMDb API で作品を検索する。

   ```
   検索:   https://api.themoviedb.org/3/search/movie?api_key=<KEY>&query=<作品名>&language=ja-JP&region=JP
   詳細:   https://api.themoviedb.org/3/movie/<id>?api_key=<KEY>&language=ja-JP
   ```

   - 検索結果は **公開年と邦題で必ず突き合わせ**、別作品を掴まないようにする
     （同名作品・リメイク・シリーズ旧作を取り違えやすい）
   - 一致したら `poster_path` を使い、`posterUrl` に
     `https://image.tmdb.org/t/p/w500<poster_path>` を入れる
   - `id` を `tmdbId` に入れる
   - TMDb に無い／確信を持って特定できない作品は **`posterUrl` を空文字にする**
     （推測で別作品の画像を入れない。空ならページ側で色付きのタイトル表示になる）
   - `runtime` `overview` は TMDb の値を使ってよい。ただし `overview` が英語のままの場合は、
     日本語の情報源から2〜3行に要約して `synopsis` に書く
   - **`config.local.json` が無い、または `tmdbApiKey` が空の場合**は、TMDb の取得を丸ごと
     スキップし、`posterUrl` を空文字のまま進める（それ自体はエラーではない）
   - **APIキーを `movies.json` に書き込まないこと**（公開リポジトリに push されるため）

5. **横浜周辺で観られるかを調べる／上映時間を確認する**

   **劇場の上映作品ページには、上映時間（○分）が載っていることが多い。**
   `runtime` が TMDb で取れなかった作品は、必ず次のページを見に行くこと。
   邦画・アニメは TMDb に情報が無いことが多いので、ここが主な情報源になる。

   - T・ジョイ横浜 https://tjoy.jp/t-joy_yokohama （作品名・上映時間・レーティングが一覧で出る）
   - 109シネマズ ムービル https://109cinemas.net/movil/ （作品ページは `109cinemas.net/movies/<番号>.html`）
   - イオンシネマ みなとみらい https://www.aeoncinema.com/cinema2/minatomirai/movie/
   - 横浜ブルク13 https://www.tjoy.net/burg13

   ただし**公開前の作品は上映時間が未発表のことが多い**。その場合は `runtime` を `null` のままにし、
   `needsCheck` は立てない（上の「情報の扱いで特に守ること」を参照）。

   - 以下の劇場で上映されているものを `theaters` に劇場名だけ入れる（上映時刻は入れない）
     - 横浜ブルク13 / T・ジョイ横浜 / TOHOシネマズ 上大岡 / TOHOシネマズ ららぽーと横浜 /
       109シネマズ港北 / イオンシネマ みなとみらい / kino cinéma 横浜みなとみらい /
       ムービル / 横浜シネマリン
   - 全国のシネコンで広く上映される大作は、確認できた劇場を2〜3館だけ挙げれば十分
   - 確認できなければ **空配列 `[]`** にする（推測で埋めない）
   - **劇場が分からなくても `needsCheck` は立てないこと。** 横浜周辺で観られない作品は
     ほとんど無く、空配列は「観られない」ではなく「調べきれなかった」という意味でしかない。
     ここで警告を出すと、本当に確認が必要な作品（特典やムビチケの不確かさ）が埋もれてしまう

6. **`updatedAt` `targetPeriod` `nextUpdate` を更新する**
   - `updatedAt` は実行日を `YYYY-MM-DD` 形式で
   - `isSampleData` が残っていれば **`false` にする**
   - `criteria` と `posterCredit` は既存の内容をそのまま維持する（勝手に書き換えない）

## 情報の扱いで特に守ること

- **入場者特典は「数量限定・なくなり次第終了」が原則**。配布終了しているものを
  「配布中」のように書かない。終了・在庫僅少の情報が見つかったら `note` に書く
- **推測で埋めない。** 確認できなかった項目は空にする
- **`needsCheck` は安売りしないこと。** カード全体がピンク色の警告になるため、乱発すると
  本当に見てほしい作品が埋もれる。`true` にしてよいのは次の2つだけ。

  1. 情報源どうしで**内容が食い違っている**とき
     （例:「公式サイトでは第2弾は10/3〜、ニュース記事では10/10〜と記載」）
  2. **まだ上映しているか自体が怪しい**とき（公開から1か月以上経過している作品など）。
     行ってから終わっていたと気づくと困るため

  **「情報が見つからなかった」だけでは `needsCheck` にしない。**
  未確認であることは、その項目の欄（`mubichike.note` / `bonusNote` / `note`）に書けば
  画面にちゃんと表示される。警告と二重に出す必要はない。
  特に **上映時間・レーティング・公式サイトURL・上映劇場が不明なだけのときは絶対に立てない**
  （公開前の作品は上映時間が未発表なことが多く、それは異常ではない）
- **`rating`（G / PG12 / R15+ / R18+）は特に厳しく扱う。**
  このサイトは「小学生以下の子どもを連れて行けるか」の判断に使われるため、
  間違った区分は実害につながる。次のどちらかで**明示的に確認できた場合だけ**書くこと。
  - 映倫（映画倫理機構）https://www.eirin.jp/ の検索結果
  - 作品の**公式サイト**に書かれている区分表記
  **TMDb の `certification` は日本の区分と一致しないことが多いので使わない。**
  上記で確認できなければ `rating` は**必ず空文字**にする。「アニメだからG」のような
  推定も禁止（実際に、ちいかわを PG12、プリキュアを PG12、ホラー作品を G と
  取り違えた事故があった）。
  **`note` や `synopsis` など他の欄に年齢区分を書くのも同じく禁止**
  （「レイティングはPG12です」とメモ欄に書いて実質的に表示させた事故があった）
- 情報源で内容が食い違う場合、**勝手に片方を採用せず** `needsCheck` を `true` にし、
  `checkReason` に不整合の内容を書く
  （例:「公式サイトでは第2弾は10/3〜、ニュース記事では10/10〜と記載」）
- ムビチケの販売終了日は「公開前日まで」が多いが作品によって違う。
  **確認できた作品だけ日付を書き**、不明なら `salesLabel` に「公式サイトで確認」と書いて
  `needsCheck` を `true` にする
- **ムビチケは原則「公開前日まで」の販売**なので、すでに公開中の作品は
  `available` を `false` にし、`note` に「公開中のため前売券の販売は終了しています」と書く。
  `available` を `true` にしてよいのは、**これから公開される作品で販売中が確認できたもの**だけ
- まとめサイト・個人ブログの情報は、**必ず公式サイトで裏を取ってから**採用する
- **ページを要約して読むと日本語のタイトルが崩れることがある**（劇場サイトの前売券一覧などで
  実際に起きた）。作品名は必ず複数の情報源で突き合わせ、少しでも怪しい表記のものは採用しない
- 情報が少ない時期は無理に本数を埋めない。正直に少ないまま出す

## movies.json のデータ構造

既存の構造を必ず維持してください。全フィールドを省略せずに書くこと。

```json
{
  "updatedAt": "2026-09-09",
  "targetPeriod": "2026年9月上旬〜10月上旬",
  "nextUpdate": "2026-09-23",
  "isSampleData": false,
  "posterCredit": "ポスター画像・作品データの一部は TMDB のものを利用しています。This product uses the TMDB API but is not endorsed or certified by TMDB.",
  "criteria": {
    "audience": "大人／こども(小学生以下)の両方",
    "area": "横浜周辺のシネコンで観られるか",
    "period": "公開中の作品が中心（直近の公開予定も少し）",
    "count": "大人向け・こども向け 各8〜12本",
    "focus": "ムビチケ前売券の販売期間・入場者特典",
    "updateCycle": "2週間に1回（隔週日曜21時に自動更新）"
  },
  "movies": [
    {
      "title": "作品名",
      "audience": "kids",
      "status": "now",
      "releaseDate": "2026-09-11",
      "releaseLabel": "2026年9月11日(金)公開",
      "rating": "G",
      "runtime": 118,
      "genre": "アニメ",
      "dubbed": true,
      "synopsis": "あらすじを2〜3行で",
      "posterUrl": "https://image.tmdb.org/t/p/w500/xxxxxxxx.jpg",
      "tmdbId": 123456,
      "mubichike": {
        "available": true,
        "salesLabel": "2026年8月1日〜9月10日(公開前日まで)",
        "types": "ムビチケカード / ムビチケオンライン券",
        "price": "一般1,600円 / 小人1,000円",
        "note": "",
        "url": "https://mubichike.jp/..."
      },
      "bonuses": [
        {
          "phase": "第1弾",
          "period": "9/11(金)〜",
          "content": "オリジナルステッカー全5種",
          "limited": true,
          "note": ""
        }
      ],
      "bonusNote": "",
      "theaters": ["横浜ブルク13", "T・ジョイ横浜"],
      "officialUrl": "https://...",
      "note": "",
      "needsCheck": false,
      "checkReason": ""
    }
  ]
}
```

### フィールドの判定基準

| フィールド | 判定基準 |
| --- | --- |
| `audience` | `"kids"` / `"adult"` / `"both"` のいずれか。親子で観られるなら必ず `"both"` |
| `status` | 公開中なら `"now"`、まだ公開前なら `"soon"` |
| `releaseDate` | 公開日を `YYYY-MM-DD` で。未定なら空文字 |
| `releaseLabel` | 画面表示用。`2026年9月11日(金)公開` のような表記 |
| `rating` | `G` / `PG12` / `R15+` / `R18+`。不明なら空文字 |
| `runtime` | 上映時間（分）。数値。不明なら `null` |
| `dubbed` | 日本語吹替版が上映されているなら `true`。邦画は `true` にせず `false`（吹替の概念が無いため） |
| `mubichike.available` | ムビチケが**販売中**なら `true`。販売終了・取扱なしなら `false` にし `note` に理由を書く |
| `mubichike.salesLabel` | 販売期間を人が読む形で。開始日が不明なら「公開前日まで販売」など分かる範囲で |
| `bonuses` | 入場者特典の配列。**第1弾・第2弾があれば分けて1件ずつ**書く。特典が無い／未発表なら空配列 `[]` |
| `bonuses[].limited` | 「数量限定」「なくなり次第終了」の記載があれば `true` |
| `bonusNote` | `bonuses` が空のときの補足（例:「特典は未発表」「第1弾は配布終了」） |
| `theaters` | 横浜周辺で上映が確認できた劇場名の配列。確認できなければ `[]` |
| `needsCheck` | 情報源の食い違い、未確定、情報不足がある場合に `true` |
| `checkReason` | `needsCheck` が `true` のとき必須。何が確認できていないかを具体的に書く |

## 完了条件

- `movies.json` が **JSONとして妥当**であること（末尾カンマ、コメント、途中で切れた出力は不可）
- ファイルは **UTF-8（BOMなし）** で保存すること
- `movies` 配列の各要素に、上記の全フィールドが揃っていること
- `posterUrl` に TMDb 以外のドメインの画像URLを入れていないこと
- APIキーが `movies.json` に混入していないこと
- 更新した内容を、最後に日本語で簡潔に要約して出力すること
  （本数・大人/こどもの内訳・対象期間・ムビチケ販売中の本数・特典ありの本数・要確認の有無）
